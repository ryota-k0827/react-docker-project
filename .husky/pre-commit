#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

CONTAINER_NAME="react-docker-project"
DC="docker compose"

function de_c {
  ${DC} up -d
  ${DC} exec -T ${CONTAINER_NAME} sh -x -c "$1"
  ${DC} down
}

de_c "yarn install \
  && npx lint-staged \
  && yarn build"
